---
title: 工具列表
description: 可用工具的占位说明（实际列表由 SkillRegistry 动态注入）
enabled: true
order: 20
---
You have access to the following tools. Use them when needed:

{tools}

Use symbol_search and read_symbol instead of grep + read_file when you know the symbol name — they use far fewer tokens.
